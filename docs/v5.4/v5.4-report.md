# v5.4 Planning Report — Content Corrections

## Source

Email thread with Victor (victor.santamag@gmail.com), subject "Re: Correction" / "Fwd:", most recent message 2026-08-28.

## Attachments referenced in the email (not yet in this repo)

Victor's 2026-08-28 message included three files described as "topo-corrections" (formatting/layout corrections), current as of that date. The Gmail tools available in this session can only read attachment *metadata* (filename/type), not download the file contents, so these still need to be saved manually into `docs/v5.4/` from the email:

- `Supplemental_spanish_hymns_hymnsapp2026_28.08.2026.txt`
- `Spanish2026_28.08.2026.txt`
- `German YP- 28.08.2026.txt`

These names line up with existing provisioning sources:
- `Spanish2026_28.08.2026.txt` → likely replaces/updates `databaseProvisioner/src/main/resources/Spanish2026.txt` (consumed by `ProvisionSpanish2026.groovy`)
- `Supplemental_spanish_hymns_hymnsapp2026_28.08.2026.txt` → likely updates the supplemental Spanish hymns consumed by `ProvisionSpanishSupplement.groovy`
- `German YP- 28.08.2026.txt` → likely updates `databaseProvisioner/src/main/resources/german/GermanYPsongs_v2.txt` (consumed by `ProvisionGermanYouth.groovy`)

**Action needed from Lemuel:** save the three attachments from the Aug 28 email into `docs/v5.4/` so we can diff them against the current provisioning resources.

## Issue #2: Wrong tune code on hymn E643 / G2163 (YP)

From the earlier message in the same thread (2026-08-11):
- Hymn **G2163 (YP)**, which corresponds to **E643**, has an incorrect tune reference.
- The app currently shows Hymnal Code `53432177615` for this hymn (stored in the `tune` column of `HymnsEntity`, `databaseProvisioner/.../lib/beans/HymnsEntity.java:50`).
- Victor flagged this as wrong but didn't provide the correct replacement value in the thread.

**Action needed from Lemuel:** confirm the correct tune code for E643/G2163 with Victor before making the change (or check against a reference hymnal if you have the correct value already).

## Planned changes for v5.4

1. Diff the three corrected files against current provisioning resources (`Spanish2026.txt`, Spanish supplemental file, `GermanYPsongs_v2.txt`) once downloaded, and apply the topo-corrections.
2. Re-run the relevant provisioning scripts (`ProvisionSpanish2026`, `ProvisionSpanishSupplement`, `ProvisionGermanYouth`) to regenerate the database with corrected content.
3. Fix the `tune` field for hymn **E643** (Hymnal Code `53432177615` → correct value, pending confirmation).
4. Bump version to v5.4 in `app/build.gradle` (currently `v5.3`) once the above are verified.

## Open questions

- Correct tune code to replace `53432177615` for E643/G2163.
- Whether "topo-corrections" in the three files touch only formatting (line breaks, stanza layout) or also wording — needs a diff once the files are in hand.
